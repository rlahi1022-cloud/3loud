#pragma once  // 헤더 중복 포함 방지

// ============================================================================
// 파일명: message_client_handle.hpp
// 설명: 클라이언트 메시지 UI 전용 핸들러 선언부
// 역할:
//   - 메시지 전송 UI 처리
//   - 서버로 JSON 패킷 송신
//   - 서버 응답 파싱 및 결과 출력
// 주의:
//   - 실제 구현은 message_client_handle.cpp에 존재
//   - 소켓은 skeleton_client에서 생성된 것을 전달받음
// ============================================================================

#include <string>

// ---------------------------------------------------------------------------
// 메시지 전송 UI 핸들러
// 기능:
//   - 사용자 입력 수집
//   - 프로토콜(JSON) 패킷 구성
//   - send_packet() 호출
//   - 서버 응답 수신 및 출력
//
// 매개변수:
//   sock : 연결된 TCP 소켓 디스크립터
// ---------------------------------------------------------------------------
void handle_message_send_ui(int sock);